# Let Nixpacks auto-detect Node.js from package.json
providers = ["node"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build",
  "npx prisma generate"
]

[start]
cmd = "npm run start:production"
