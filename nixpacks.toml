# Explicitly define Node.js setup for Railway compatibility
[phases.setup]
nixPkgs = ["nodejs_18", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build",
  "npx prisma generate"
]

[start]
cmd = "bash scripts/railway-deploy.sh && npm run start:production"
